// The build package is used by the --version command. Actual Version and SHA information is injected during the release
// build pipeline

package build

var Version = "manualBuild"

var SHA = "manualBuild"
